[project]
name = "rent-etl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"


[dependency-groups]
prod = [
    "pyspark>=4.1.1",
]
dev = [
    "pyspark>=4.1.1",
    "pytest>=9.0.2",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.10",
]
